services:
  hub:
    build: .
    container_name: honeywire-hub
    restart: unless-stopped
    ports:
      - "${HUB_PORT}:8080"
    volumes:
      - honeywire_data:/data
    env_file: 
      - .env
      
volumes:
  honeywire_data: